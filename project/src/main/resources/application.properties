# Application Info
spring.application.name=Web Based Train Reservation System

# Server Configuration
server.port=8080

# ===== H2 IN-MEMORY DATABASE (FOR DEVELOPMENT ONLY) =====
spring.datasource.url=jdbc:h2:mem:slrailedb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=jaksayura@gmailcom
spring.datasource.password=65307144   # ? FIXED: Removed space between 6530 and 7144

# JPA / Hibernate Settings
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Enable H2 Console for debugging
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Application URL (for email links or API references)
app.url=http://localhost:8080

# Logging ? USE YOUR PACKAGE NAME, NOT com.slraile!
logging.level._Y2_S1_MTR_02=DEBUG
logging.level.org.h2=DEBUG