# Application Info
spring.application.name=Web Based Train Reservation System

# Server Configuration
server.port=8080

# -- Azure SQL Database Connection Settings --
# Paste the full JDBC string you copied from Azure here
spring.datasource.url=jdbc:sqlserver://train-reserevation-system.database.windows.net:1433;database=Train Reservation System;user=db_admin@train-reserevation-system;password=Soesh@123;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;

# Use the username and password you created for the Azure server
spring.datasource.username=db_admin
spring.datasource.password=Soesh@123

# -- JPA / Hibernate Settings --
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
# 'update' is okay for development. Use 'validate' or 'none' later.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Application URL (for email links or API references)
app.url=http://localhost:8080

# Logging ? USE YOUR PACKAGE NAME, NOT com.slraile!
logging.level._Y2_S1_MTR_02=DEBUG
logging.level.org.h2=DEBUG

# --- Default Admin Credentials (for seeding) ---
app.admin.email=admin@gmail.com
app.admin.password=Admin@123
app.admin.fullname=Administrator
app.admin.phone=0000000000
app.admin.reset-on-startup=true

# --- Mail Configuration ---
# Using Gmail SMTP (you can change this to your preferred email provider)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=your-email@gmail.com
spring.mail.password=your-app-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true